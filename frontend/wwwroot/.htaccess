<IfModule mod_rewrite.c>
	RewriteEngine On

	# Redirect requests to index.html if file or directory doesn't exist
	RewriteCond %{REQUEST_FILENAME} !-f
	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteRule ^ index.html [L]
</IfModule>